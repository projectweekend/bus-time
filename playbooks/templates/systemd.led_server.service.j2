[Unit]
Description={{ item.name }}
After=network.target

[Service]
Type=simple
WorkingDirectory=/home/pi/bus_time_alert
ExecStart=/usr/local/bin/gunicorn -b 127.0.0.1:8000 led_server_{{ item.name }}
Restart=always

[Install]
WantedBy=multi-user.target
