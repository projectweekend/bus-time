[Unit]
Description={{ item.name }}
After=network.target

[Service]
Type=simple
WorkingDirectory=/home/pi
ExecStart=/usr/bin/gunicorn -b 127.0.0.1:8000 bus_time_alert/led_server_{{ item.name }}
Restart=always

[Install]
WantedBy=multi-user.target
