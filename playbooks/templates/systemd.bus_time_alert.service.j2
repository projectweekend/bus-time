[Unit]
Description={{ item.name }}
After=network.target

[Service]
Type=simple
WorkingDirectory=/home/pi
ExecStart=/usr/bin/python3 bus_time_alert/main.py bus_time_alert/{{ item.name }}.json
Restart=always

[Install]
WantedBy=multi-user.target
